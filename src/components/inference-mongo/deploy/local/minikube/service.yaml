apiVersion: v1
kind: Service
metadata:
  name: mlhub-mongodb-service
spec:
  type: ClusterIP
  selector:
    app: mlhub-mongodb-deployment
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
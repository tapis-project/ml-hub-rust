[package]
name = "git-lfs-client"
version = "0.1.0"
edition = "2021"

[dependencies]
shared = { version = "0.1.0", path = "../shared" }
clients = { version = "0.1.0", path = "../clients" }
async-trait = "0.1.88"

[package]
name = "git-lfs-client"
version = "0.1.0"
edition = "2021"

[dependencies]
shared = { version = "0.1.0", path = "../shared" }
clients = { version = "0.1.0", path = "../clients" }
platforms = { version = "0.1.0", path = "../platforms" }
async-trait = "0.1.88"

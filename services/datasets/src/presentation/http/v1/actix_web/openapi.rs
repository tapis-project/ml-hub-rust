use platforms::Platform;
use shared::presentation::http::v1::responses::{Artifact, ArtifactPublication, ArtifactIngestion};
use utoipa::OpenApi;
use crate::config::VERSION;

// Here we are import the helper function generated by utoipa to generate docs for
// handlers
use super::handlers::health_check::__path_health_check;

#[derive(OpenApi)]
#[openapi(
    paths(
        health_check,
    ),
    components(
        schemas(
            Platform,
            Artifact,
            ArtifactPublication,
            ArtifactIngestion
        )
    ),
    info(title = "MLHub Datasets API", version=VERSION)
)]
pub struct ApiDoc;
# MLHub Deployments Collection Migrator

This program applies MongoDB schema migrations owned by the Deployments API. It creates the HPC cluster collection and indexes but does not seed cluster definitions.

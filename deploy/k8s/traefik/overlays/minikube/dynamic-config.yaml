http:
  middlewares:
    tapis-rewrite-host:
      headers:
        customRequestHeaders:
          Host: "tacc.develop.tapis.io"
    cors:
      headers:
        accessControlAllowOriginList:
          - http://localhost:3000
        accessControlAllowMethods:
          - GET
          - POST
          - PUT
          - DELETE
          - OPTIONS
        accessControlAllowHeaders:
          - Content-Type
          - Authorization
          - X-Tapis-Token
        accessControlAllowCredentials: true
  routers:
    dashboard:
      rule: (PathPrefix(`/api`) || PathPrefix(`/dashboard`))
      service: api@internal

    models-api:
      rule: "Host(`tacc.local.develop.tapis.io`) && PathPrefix(`/models-api`)"
      service: models-api
      middlewares:
        - tapis-rewrite-host
        - cors
    
    datasets-api:
      rule: "HostRegexp(`tacc.local.develop.tapis.io`) && PathPrefix(`/datasets-api`)"
      service: datasets-api
      middlewares:
        - tapis-rewrite-host
        - cors

    deployments-api:
      rule: "Host(`tacc.local.develop.tapis.io`) && PathPrefix(`/deployments-api`)"
      service: deployments-api
      middlewares:
        - tapis-rewrite-host
        - cors

    agents-api:
      rule: "Host(`tacc.local.develop.tapis.io`) && PathPrefix(`/agents-api`)"
      service: agents-api
      middlewares:
        - tapis-rewrite-host
        - cors

  services:
    models-api:
      loadBalancer:
        servers:
        - url: http://mlhub-models-api-service:8000
    deployments-api:
      loadBalancer:
        servers:
        - url: http://mlhub-deployments-api-service:8000
    datasets-api:
      loadBalancer:
        servers:
        - url: http://mlhub-datasets-api-service:8000
    agents-api:
      loadBalancer:
        servers:
        - url: http://mlhub-agents-api-service:8000

apiVersion: v1
kind: Service
metadata:
  name: mlhub-mongo-service
spec:
  type: ClusterIP
  selector:
    app: mlhub-mongo-deployment
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
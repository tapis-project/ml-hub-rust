apiVersion: apps/v1
kind: Deployment
metadata:
  name: mlhub-agents-api-deployment
spec:
  selector:
    matchLabels:
      app: mlhub-agents-api-deployment
  template:
    metadata:
      labels:
        app: mlhub-agents-api-deployment
    spec:
      containers:
      - env:
          - name: DB_HOST
            value: mlhub-mongo-service
          - name: DB_NAME
            value: mlhub
          - name: DB_PASSWORD
            valueFrom:
              secretKeyRef:
                name: mlhub-mongo-secrets
                key: mongo-user-password
          - name: DB_USER
            value: mlhub
          - name: RUST_LOG
            value: debug
        image: tapis/agents-api:local
        imagePullPolicy: Never
        name: agents-api
        ports:
          - containerPort: 8000
            name: webserver
        resources: {}
        tty: true

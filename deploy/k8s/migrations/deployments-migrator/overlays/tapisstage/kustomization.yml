resources:
  - ../../base

patches:
  - target:
      kind: Job
      name: mlhub-deployments-migrator
    patch: |-
      apiVersion: batch/v1
      kind: Job
      metadata:
        name: mlhub-deployments-migrator
      spec:
        template:
          spec:
            containers:
              - name: deployments-migrator
                image: tapis/mlhub-deployments-migrator:staging
                imagePullPolicy: Always
                resources: {}
                env:
                  - name: MONGO_HOST
                    value: mlhub-mongo-stateful-set-0.mlhub-mongo-headless-service.tapisstage.svc.cluster.local

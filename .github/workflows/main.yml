name: Tapis MLHub CI

on:
  workflow_dispatch:
    inputs:
      force_rebuild_all:
        type: boolean
        default: true
  push:
    branches: [ dev, staging, test, main ]
    tags:
      - 'v*.*.*'
      - 'v*'
  pull_request:
    branches: [ dev, staging, test, main ]

jobs:
  resolve_image_tag:
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.resolve.outputs.image_tag }}
    steps:
      - id: resolve
        name: Set_image
        shell: bash
        run: |
          # If the $GITHUB_REF contains "/refs/tags/v", then the "v" needs to be
          # removed from the $GITHUB_REF_NAME to form a proper image tag
          if [[ $GITHUB_REF =~ "/refs/tags/v"  ]]; then 
            echo image_tag=$(echo $GITHUB_REF_NAME | sed -e 's!v!!') >> $GITHUB_OUTPUT
          else
            echo "image_tag=$GITHUB_REF_NAME" >> $GITHUB_OUTPUT;
          fi

  diffs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/detect-service-diffs

  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
      - name: Run Tests
        run: cargo test --workspace
        

  models:
    # if: contains(fromJson(needs.diffs.outputs.services), 'models') || github.event.inputs.force_rebuild_all == 'true'
    needs:
      - resolve_image_tag
      - diffs
      - tests
    secrets: inherit
    uses: ./.github/workflows/models.yml
    with:
      image_name: tapis/mlhub-models-api
      image_tag:  ${{ needs.resolve_image_tag.outputs.image_tag }}
  
  
  deployments:
    # if: contains(fromJson(needs.diffs.outputs.services), 'deployments') || github.event.inputs.force_rebuild_all == 'true'
    needs:
      - resolve_image_tag
      - diffs
      - tests
    secrets: inherit
    uses: ./.github/workflows/deployments.yml
    with:
      image_name: tapis/mlhub-deployments-api
      image_tag:  ${{ needs.resolve_image_tag.outputs.image_tag }}

  model-deployment-controller:
    # if: contains(fromJson(needs.diffs.outputs.services), 'model-deployment-controller') || github.event.inputs.force_rebuild_all == 'true'
    needs:
      - resolve_image_tag
      - diffs
      - tests
    secrets: inherit
    uses: ./.github/workflows/model-deployment-controller.yml
    with:
      image_name: tapis/mlhub-model-deployment-controller
      image_tag:  ${{ needs.resolve_image_tag.outputs.image_tag }}

  artifact-ingester:
    # if: contains(fromJson(needs.diffs.outputs.services), 'artifact-ingester') || github.event.inputs.force_rebuild_all == 'true'
    needs:
      - resolve_image_tag
      - diffs
      - tests
    secrets: inherit
    uses: ./.github/workflows/artifact-ingester.yml
    with:
      image_name: tapis/mlhub-artifact-publisher
      image_tag:  ${{ needs.resolve_image_tag.outputs.image_tag }}

  artifact-publisher:
    # if: contains(fromJson(needs.diffs.outputs.services), 'artifact-publisher') || github.event.inputs.force_rebuild_all == 'true'
    needs:
      - resolve_image_tag
      - diffs
      - tests
    secrets: inherit
    uses: ./.github/workflows/artifact-publisher.yml
    with:
      image_name: tapis/mlhub-artifact-ingester
      image_tag:  ${{ needs.resolve_image_tag.outputs.image_tag }}

  models-migrator:
    # if: contains(fromJson(needs.diffs.outputs.services), 'models-migrator') || github.event.inputs.force_rebuild_all == 'true'
    needs:
      - resolve_image_tag
      - diffs
      - tests
    secrets: inherit
    uses: ./.github/workflows/models-migrator.yml
    with:
      image_name: tapis/mlhub-models-migrator
      image_tag:  ${{ needs.resolve_image_tag.outputs.image_tag }}

  federated-identities-migrator:
    # if: contains(fromJson(needs.diffs.outputs.services), 'federated-identities-migrator') || github.event.inputs.force_rebuild_all == 'true'
    needs:
      - resolve_image_tag
      - diffs
      - tests
    secrets: inherit
    uses: ./.github/workflows/federated-identities-migrator.yml
    with:
      image_name: tapis/mlhub-federated-identities-migrator
      image_tag:  ${{ needs.resolve_image_tag.outputs.image_tag }}
  
  principals-migrator:
    # if: contains(fromJson(needs.diffs.outputs.services), 'principals-migrator') || github.event.inputs.force_rebuild_all == 'true'
    needs:
      - resolve_image_tag
      - diffs
      - tests
    secrets: inherit
    uses: ./.github/workflows/principals-migrator.yml
    with:
      image_name: tapis/mlhub-principals-migrator
      image_tag:  ${{ needs.resolve_image_tag.outputs.image_tag }}

  hf-model-etl:
    # if: contains(fromJson(needs.diffs.outputs.services), 'principals-migrator') || github.event.inputs.force_rebuild_all == 'true'
    needs:
      - resolve_image_tag
      - diffs
      - tests
    secrets: inherit
    uses: ./.github/workflows/huggingface-model-metadata-etl.yml
    with:
      extract_stage_image_name: tapis/mlhub-hf-model-etl-extract
      extract_stage_image_tag:  ${{ needs.resolve_image_tag.outputs.image_tag }}
      transform_load_stage_image_name: tapis/mlhub-hf-model-etl-transform-load
      transform_load_stage_image_tag:  ${{ needs.resolve_image_tag.outputs.image_tag }}


  

  # HF_Model_ETL:
  #   needs:
  #     - resolve_image_tag
  #   name: HF_Model_ETL_Extract_Compile_Test_Build_Push
  #   runs-on: ubuntu-latest
  #   steps:
  #     # Checkout the code
  #     - name: Checkout
  #       uses: actions/checkout@v3

  #     # Resolve all diffs if any for this service
  #     - id: git-diff
  #       name: Check Git Diff
  #       uses: technote-space/get-diff-action@v6
  #       with:
  #         PATTERNS: |
  #           service/hf-model-etl/**/*

  #     # Setup rust
  #     - name: Setup Rust
  #       run: rustup toolchain install stable
  #       if: steps.git-diff.outputs.diff

  #     # # Compile and run tests
  #     # - name: Test
  #     #   run: "./manage test models -sv"
  #     #   if: steps.git-diff.outputs.diff

  #     # Compile the release binary of the api
  #     - name: Compile
  #       run: "./manage install hf-model-etl -sv"
  #       if: steps.git-diff.outputs.diff

  #     # Log into dockerhub with CIC credentials
  #     - name: Login to Docker Hub
  #       uses: docker/login-action@v1
  #       with:
  #         username: ${{ secrets.CIC_DOCKERHUB_USERNAME }}
  #         password: ${{ secrets.CIC_DOCKERHUB_ACCESS_TOKEN }}
  #       if: steps.git-diff.outputs.diff

  #     # Remove the docker ignore file so that the gh actions Dockerfile will 
  #     # permit the target/release/<service> binary to be copied into the image
  #     - name: Delete docker ignore files 
  #       run: rm .dockerignore service/hf-model-etl/.dockerignore
  #       if: steps.git-diff.outputs.diff

  #     # Build the image for the extract step
  #     - name: Build_Extract_Image
  #       run: "./manage build-extract hf-model-etl -sv -t extract-img tapis/mlhub-hf-model-etl-extract -t tag ${{ needs.resolve_image_tag.outputs.image_tag }}"
  #       if: steps.git-diff.outputs.diff

  #     # Build the image for the transform-load step
  #     - name: Build_Transform_Load_Image
  #       run: "./manage build-transform-load hf-model-etl -sv -t transform-load-img tapis/mlhub-hf-model-etl-transform-load -t env gh-actions -t tag ${{ needs.resolve_image_tag.outputs.image_tag }}"
  #       if: steps.git-diff.outputs.diff

  #     # Push the all tags for the image
  #     - name: Push All Tags
  #       run: ./manage pusht hf-model-etl -sv -t transform-load-img tapis/mlhub-hf-model-etl-transform-load -t extract-img tapis/mlhub-hf-model-etl-extract
  #       if: steps.git-diff.outputs.diff